import logo from "@/assets/logo.png";

const Logo = () => {
  return (
    <img
      src={logo}
      alt="לוגו"
      style={{
        height: "90px",
        width: "90px",
      }}
    />
  );
};

export default Logo;
